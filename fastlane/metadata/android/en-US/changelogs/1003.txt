Fixed the startup crash introduced in 0.1.0: the minified release build let R8 strip Moshi's generated JSON adapters and rename enum constants, which the JSON layer resolves reflectively. Release builds now start normally again.
