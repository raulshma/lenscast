<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.BIND_QUICK_SETTINGS_TILE" />
    <uses-permission android:name="com.raulshma.lenscast.permission.AUTOMATION" />

    <!--
      Guards the exported AutomationReceiver, so arbitrary apps cannot drive
      the camera, the recorder, or the siren. Dangerous level: a sender app
      is granted it only after a runtime request (Android 6+), never at
      install; adb sends as the exempt shell uid.
    -->
    <permission
        android:name="com.raulshma.lenscast.permission.AUTOMATION"
        android:label="@string/automation_permission_label"
        android:description="@string/automation_permission_description"
        android:protectionLevel="dangerous" />

    <application
        android:name=".MainApplication"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LensCast"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="36">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.LensCast">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".capture.RecordingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="camera|microphone" />

        <service
            android:name=".streaming.StreamingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="camera|microphone" />

        <service
            android:name=".streaming.StreamTileService"
            android:exported="true"
            android:icon="@android:drawable/ic_menu_camera"
            android:label="Streaming"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".streaming.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--
          External automation surface (Tasker/MacroDroid/adb). Exported, but
          guarded by a dangerous custom permission (declared at manifest
          level, above): an unguarded exported receiver would let any
          installed app silently start the camera, trigger recordings, or
          fire the siren. Automation senders declare
          `<uses-permission android:name="com.raulshma.lenscast.permission.AUTOMATION" />`
          (dangerous level, so a helper app must also request it at runtime,
          like camera access — it is not granted at install);
          adb sends as shell/root, which the system exempts. This app holds
          its own declared permission for the widget's explicit broadcasts.
        -->
        <receiver
            android:name=".automation.AutomationReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="com.raulshma.lenscast.permission.AUTOMATION">
            <intent-filter>
                <action android:name="com.raulshma.lenscast.action.START_STREAM" />
                <action android:name="com.raulshma.lenscast.action.STOP_STREAM" />
                <action android:name="com.raulshma.lenscast.action.CAPTURE_PHOTO" />
                <action android:name="com.raulshma.lenscast.action.START_RECORDING" />
                <action android:name="com.raulshma.lenscast.action.STOP_RECORDING" />
                <action android:name="com.raulshma.lenscast.action.SET_TORCH" />
                <action android:name="com.raulshma.lenscast.action.SET_SIREN" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".streaming.StreamWidgetProvider"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/stream_widget_info" />
        </receiver>

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="camera"
            tools:node="merge" />

    </application>
</manifest>
